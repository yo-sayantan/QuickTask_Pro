name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      - name: Install Dependencies
        run: flutter pub get
        working-directory: QuickTask_app

      - name: Build APK
        run: flutter build apk --release
        working-directory: QuickTask_app

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: QuickTask-Pro-apk
          path: QuickTask_app/build/app/outputs/flutter-apk/app-release.apk
